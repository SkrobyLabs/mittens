# Go source and build artifacts (not needed in Docker build context)
*.go
go.mod
go.sum
internal/
extensions/registry/
extensions/*/resolver.go
extensions/*/*.go

# Git and IDE
.git
.gitignore
.claude/
.dockerignore

# Documentation
README.md
*.md

# Bash CLI (legacy, not needed for Docker build)
mittens
wizard.sh
install.sh
